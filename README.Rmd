---
title: "Learn Statistical"
author: "GongCC"
date: "`r Sys.Date()`"
output:
        github_document
editor_options: 
  chunk_output_type: console
---

## Part One: Perceptron



```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

===============Example One===============

```{r}
library(learnML)
X <- iris[1:100,1:2]
y <- iris$Species[1:100]
m <- Perceptron$new(max_iteration = 10000)
m$fit(X, y)
```


```{r}
m$plot()
```

===============Example Two ===============

```{r}
library(learnML)
X <- matrix(c(3, 3, 4, 3, 1, 1), ncol = 2, byrow = TRUE)
y <- c(1, 1, -1)
fit <- Perceptron$new()
fit$fit(X, y, w = c(0,0), b = 0)
```


```{r}
fit$plot()
```

===============Example Three ===============
```{r}
X <- iris[1:100,1:2]
y <- iris$Species[1:100]
set.seed(123)
idx <- sample(100, 50, replace = FALSE)
m <- Perceptron$new()
m$fit(X[idx, ], y[idx])
```

```{r}
m$plot()
```

```{r}
pre <- m$predict(X[-idx,], y[-idx])
# pre
```

```{r}
m$plot(X[-idx, ], y[-idx])
```

===============Example Four ===============

```{r}
X <- iris[1:100,1:4]
y <- iris$Species[1:100]
set.seed(123)
idx <- sample(100, 50, replace = FALSE)
m <- Perceptron$new()
m$fit(X[idx, ], y[idx], seed = 888)
```

```{r}
pre <- m$predict(X[-idx,], y[-idx])
```

```{r}
m$model
```

$`r simsalapar::expr2latex(expr = quote(-0.15 * w[1] + -0.67 * w[2] + 2.08 * w[3] + 0.17 * w[4] + -1.36 == 0))`$
